[providers]
# Flutter needs Node.js for web
nodejs = "18"
flutter = "3.19"

[build]
# Install dependencies and build web
cmds = [
  "flutter pub get",
  "flutter build web --release"
]

[start]
cmd = "npm install -g serve && serve -s build/web -l $PORT"
